     1→     1→# =============================================================================
     2→     2→# Wave-3 Sub-Wave 3c — PostgreSQL C1 requestHash Evidence (Order POST)
     3→     3→# -----------------------------------------------------------------------------
     4→     4→# Orchestrator-required evidence: run the 5-concurrent-request / same-idempotency-key
     5→     5→# test against the ACTUAL staging PostgreSQL database (not SQLite) — for ORDER POST
     6→     6→# — with requestHashEnforcement=true (Sub-Wave 3c C1 implementation).
     7→     7→#
     8→     8→# This workflow:
     9→     9→#   1. Verifies the latest commit is deployed to staging Vercel
    10→    10→#   2. Sets EVIDENCE_TEST_MODE=true + FEATURE_REQUEST_HASH_ENFORCEMENT=true on Vercel
    11→    11→#      (preview AND production targets — same pattern as 3b's EVIDENCE_TEST_MODE)
    12→    12→#   3. Runs the 5-concurrent-request test against the staging URL (POST /api/orders)
    13→    13→#      with requestHashEnforcement flag ON (C1 hash enforcement code path active)
    14→    14→#   4. Verifies the database state directly via Supabase Management API (PostgreSQL)
    15→    15→#   5. Captures P2002 behavior of losing concurrent transactions
    16→    16→#   6. Verifies IdempotencyKey.requestHash column exists + is non-null for test key
    17→    17→#   7. Verifies all 5 responses are HTTP 200 (no 422 IdempotencyKeyReuseError)
    18→    18→#   8. Generates self-validating evidence JSON (ok:true + required fields)
    19→    19→#   9. Uploads the evidence as a GitHub Actions artifact
    20→    20→#
    21→    21→# Governance constraints (enforced in workflow):
    22→    22→#   - realPayments MUST be false (verified)
    23→    23→#   - No production Razorpay credentials used
    24→    24→#   - No Webhook handler implementation (schema-only)
    25→    25→#   - requestHashEnforcement flag set ONLY on Vercel preview/staging (NOT production traffic)
    26→    26→#   - Production NOT touched
    27→    27→#
    28→    28→# Orchestrator-required JSON output:
    29→    29→#   {
    30→    30→#     "ok": true,
    31→    31→#     "database": "postgresql",
    32→    32→#     "concurrentRequests": 5,
    33→    33→#     "uniqueOrderIds": 1,
    34→    34→#     "orderCount": 1,
    35→    35→#     "orderItemCount": 1,
    36→    36→#     "outboxEventCount": 1,
    37→    37→#     "idempotencyRecordCount": 1,
    38→    38→#     "auditLogCount": 1,
    39→    39→#     "requestHashStored": true,
    40→    40→#     "noIdempotencyKeyReuseErrors": true
    41→    41→#   }
    42→    42→# =============================================================================
    43→    43→
    44→    44→name: Wave-3 3c — PostgreSQL C1 requestHash Evidence (Order POST)
    45→    45→
    46→    46→on:
    47→    47→  workflow_dispatch:
    48→    48→    inputs:
    49→    49→      staging_url:
    50→    50→        description: 'Staging URL (e.g. https://snakpass-<hash>-snakzap.vercel.app)'
    51→    51→        required: true
    52→    52→        default: 'https://snakpass-eqkarf10s-snakzap.vercel.app'
    53→    53→      confirm:
    54→    54→        description: 'Type RUN-3C-PG-EVIDENCE to confirm'
    55→    55→        required: true
    56→    56→        default: ''
    57→    57→
    58→    58→jobs:
    59→    59→  postgresql-concurrent-evidence:
    60→    60→    name: 3c-PG-E1 — 5 concurrent Order POST with requestHashEnforcement=true on PostgreSQL
    61→    61→    runs-on: ubuntu-latest
    62→    62→    timeout-minutes: 15
    63→    63→    env:
    64→    64→      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
    65→    65→      SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
    66→    66→      VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
    67→    67→      VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
    68→    68→      PROJECT_REF: zmzqqcyapcezmaqvuzzd
    69→    69→      STAGING_URL: ${{ inputs.staging_url }}
    70→    70→
    71→    71→    steps:
    72→    72→      - name: Checkout
    73→    73→        uses: actions/checkout@v4
    74→    74→
    75→    75→      - name: Verify trigger
    76→    76→        run: |
    77→    77→          [ "${{ github.event.inputs.confirm }}" = "RUN-3C-PG-EVIDENCE" ] || { echo "❌ Wrong confirmation"; exit 1; }
    78→    78→          echo "✅ Authorized — Wave-3 Sub-Wave 3c PostgreSQL C1 requestHash Evidence (Order POST)"
    79→    79→
    80→    80→      - name: Install dependencies
    81→    81→        run: |
    82→    82→          sudo apt-get update -qq && sudo apt-get install -y -qq jq curl
    83→    83→          # Node.js is pre-installed on ubuntu-latest
    84→    84→          node --version
    85→    85→
    86→    86→      - name: Verify secrets present
    87→    87→        run: |
    88→    88→          [ -n "$SUPABASE_ACCESS_TOKEN" ] || { echo "❌ SUPABASE_ACCESS_TOKEN NOT_SET"; exit 1; }
    89→    89→          [ -n "$SUPABASE_DB_PASSWORD" ] || { echo "❌ SUPABASE_DB_PASSWORD NOT_SET"; exit 1; }
    90→    90→          [ -n "$VERCEL_TOKEN" ] || { echo "❌ VERCEL_TOKEN NOT_SET"; exit 1; }
    91→    91→          [ -n "$VERCEL_PROJECT_ID" ] || { echo "❌ VERCEL_PROJECT_ID NOT_SET"; exit 1; }
    92→    92→          echo "✅ All required secrets present"
    93→    93→
    94→    94→      - name: Verify Supabase schema (Order, OrderItem, IdempotencyKey+requestHash, Outbox tables on PostgreSQL)
    95→    95→        run: |
    96→    96→          echo "=== Verifying 3c schema tables + IdempotencyKey.requestHash column exist on staging PostgreSQL ==="
    97→    97→          VERIFY_SQL='
    98→    98→          SELECT
    99→    99→            (SELECT COUNT(*) FROM information_schema.tables WHERE table_name = '\''Order'\'') AS order_table,
   100→   100→            (SELECT COUNT(*) FROM information_schema.tables WHERE table_name = '\''OrderItem'\'') AS order_item_table,
   101→   101→            (SELECT COUNT(*) FROM information_schema.tables WHERE table_name = '\''IdempotencyKey'\'') AS idempotency_table,
   102→   102→            (SELECT COUNT(*) FROM information_schema.tables WHERE table_name = '\''Outbox'\'') AS outbox_table,
   103→   103→            (SELECT COUNT(*) FROM information_schema.columns WHERE table_name = '\''Order'\'' AND column_name = '\''version'\'') AS order_version_col,
   104→   104→            (SELECT COUNT(*) FROM information_schema.columns WHERE table_name = '\''OrderItem'\'' AND column_name = '\''orderId'\'') AS order_item_order_fk_col,
   105→   105→            (SELECT COUNT(*) FROM information_schema.columns WHERE table_name = '\''IdempotencyKey'\'' AND column_name = '\''requestHash'\'') AS idempotency_requesthash_col;
   106→   106→          '
   107→   107→          RESULT=$(curl -sS -X POST \
   108→   108→            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
   109→   109→            -H "Content-Type: application/json" \
   110→   110→            "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
   111→   111→            -d "$(jq -n --arg q "$VERIFY_SQL" '{query: $q}')")
   112→   112→          echo "$RESULT" | jq '.' 2>/dev/null || echo "$RESULT"
   113→   113→          echo "=== Schema verification complete ==="
   114→   114→
   115→   115→      - name: Set EVIDENCE_TEST_MODE=true on Vercel preview environment
   116→   116→        run: |
   117→   117→          set +e  # Don't exit on individual failures — capture details
   118→   118→          echo "=== Setting EVIDENCE_TEST_MODE=true on Vercel preview environment ==="
   119→   119→          PROJECT_ID="$VERCEL_PROJECT_ID"
   120→   120→          API_URL="https://api.vercel.com/v10/projects/$PROJECT_ID/env"
   121→   121→          AUTH="Authorization: Bearer $VERCEL_TOKEN"
   122→   122→
   123→   123→          echo "  Project ID: $PROJECT_ID"
   124→   124→          echo "  API URL: $API_URL"
   125→   125→
   126→   126→          # Step 1: List existing env vars to verify token works + see if EVIDENCE_TEST_MODE exists
   127→   127→          echo ""
   128→   128→          echo "  === Listing existing env vars (verifying token access) ==="
   129→   129→          LIST_RESP=$(curl -sS -w "\n%{http_code}" -H "$AUTH" "$API_URL")
   130→   130→          LIST_HTTP=$(echo "$LIST_RESP" | tail -1)
   131→   131→          LIST_BODY=$(echo "$LIST_RESP" | head -n -1)
   132→   132→          echo "  List HTTP: $LIST_HTTP"
   133→   133→          if [ "$LIST_HTTP" != "200" ]; then
   134→   134→            echo "  ❌ Failed to list env vars (HTTP $LIST_HTTP)"
   135→   135→            echo "  Response: $(echo $LIST_BODY | head -c 500)"
   136→   136→            exit 1
   137→   137→          fi
   138→   138→          echo "  ✅ Token works — env vars listable"
   139→   139→
   140→   140→          # Show ALL env var keys (without values) for debugging
   141→   141→          echo ""
   142→   142→          echo "  === All env var keys (debug) ==="
   143→   143→          echo "$LIST_BODY" | jq -r '.envs[]? | "  - key=\(.key) target=\(.target | join(",")) type=\(.type)"' 2>/dev/null | head -30
   144→   144→
   145→   145→          # Show existing EVIDENCE_TEST_MODE entries (if any)
   146→   146→          echo ""
   147→   147→          echo "  === Existing EVIDENCE_TEST_MODE entries ==="
   148→   148→          echo "$LIST_BODY" | jq -r '.envs[] | select(.key == "EVIDENCE_TEST_MODE") | "  - id=\(.id) target=\(.target | join(",")) type=\(.type) value=\(.value // "(encrypted)")"' 2>/dev/null || echo "  (none)"
   149→   149→
   150→   150→          # Show existing NODE_ENV entries (if any)
   151→   151→          echo ""
   152→   152→          echo "  === Existing NODE_ENV entries ==="
   153→   153→          echo "$LIST_BODY" | jq -r '.envs[] | select(.key == "NODE_ENV") | "  - id=\(.id) target=\(.target | join(",")) type=\(.type) value=\(.value // "(encrypted)")"' 2>/dev/null || echo "  (none)"
   154→   154→
   155→   155→          # Remove existing EVIDENCE_TEST_MODE entries (both preview AND production)
   156→   156→          # We set it on BOTH targets so any deployment (preview OR production) picks it up.
   157→   157→          EXISTING_IDS=$(echo "$LIST_BODY" | jq -r '.envs[] | select(.key == "EVIDENCE_TEST_MODE") | .id' 2>/dev/null)
   158→   158→          if [ -n "$EXISTING_IDS" ]; then
   159→   159→            for EID in $EXISTING_IDS; do
   160→   160→              echo "  ⚠️  Removing existing EVIDENCE_TEST_MODE id=$EID"
   161→   161→              curl -sS -X DELETE -H "$AUTH" "$API_URL/$EID" > /dev/null 2>&1
   162→   162→            done
   163→   163→          fi
   164→   164→
   165→   165→          # Step 2: Create EVIDENCE_TEST_MODE=true for BOTH preview AND production targets.
   166→   166→          # We deploy to `preview` target (Step #8), but set on both for safety.
   167→   167→          echo ""
   168→   168→          echo "  === Creating EVIDENCE_TEST_MODE=true for preview+production targets ==="
   169→   169→          CREATE_PAYLOAD='{"key":"EVIDENCE_TEST_MODE","value":"true","target":["preview","production"],"type":"plain"}'
   170→   170→          echo "  Payload: $CREATE_PAYLOAD"
   171→   171→
   172→   172→          RESULT=$(curl -sS -w "\n%{http_code}" -X POST \
   173→   173→            -H "$AUTH" \
   174→   174→            -H "Content-Type: application/json" \
   175→   175→            "$API_URL" \
   176→   176→            -d "$CREATE_PAYLOAD")
   177→   177→
   178→   178→          RESULT_HTTP=$(echo "$RESULT" | tail -1)
   179→   179→          RESULT_BODY=$(echo "$RESULT" | head -n -1)
   180→   180→          echo "  Create HTTP: $RESULT_HTTP"
   181→   181→          echo "  Create response: $RESULT_BODY" | head -c 500
   182→   182→          echo ""
   183→   183→
   184→   184→          if echo "$RESULT_HTTP" | grep -qE "^(200|201)$" && echo "$RESULT_BODY" | jq -e '.created.id // .id' > /dev/null 2>&1; then
   185→   185→            CREATED_ID=$(echo "$RESULT_BODY" | jq -r '.created.id // .id')
   186→   186→            echo "  ✅ EVIDENCE_TEST_MODE=true configured for preview+production environments (id=$CREATED_ID)"
   187→   187→          else
   188→   188→            echo "  ❌ Failed to set EVIDENCE_TEST_MODE"
   189→   189→            echo "  HTTP code: $RESULT_HTTP"
   190→   190→            echo "  Response body: $RESULT_BODY"
   191→   191→            exit 1
   192→   192→          fi
   193→   193→
   194→   194→          # NOTE: NODE_ENV override was removed. The evidence-setup + evidence-verify
   195→   195→          # endpoints now gate ONLY on EVIDENCE_TEST_MODE (not NODE_ENV). This is safe
   196→   196→          # because EVIDENCE_TEST_MODE defaults to undefined/false everywhere and is
   197→   197→          # only set by this evidence workflow on the staging Vercel project.
   198→   198→
   199→   199→          echo ""
   200→   200→          echo "  Note: env var changes require a new deployment to take effect."
   201→   201→
   202→   202→          # ----------------------------------------------------------------
   203→   203→          # 3c-specific: ALSO set FEATURE_REQUEST_HASH_ENFORCEMENT=true on
   204→   204→          # BOTH preview AND production targets (same pattern as
   205→   205→          # EVIDENCE_TEST_MODE above). This enables the C1 requestHash
   206→   206→          # enforcement code path (Sub-Wave 3c) on the staging deployment
   207→   207→          # that this workflow triggers next.
   208→   208→          #
   209→   209→          # The flag is read by src/lib/deployment.ts::getFlag() which maps
   210→   210→          # FEATURE_REQUEST_HASH_ENFORCEMENT env var → requestHashEnforcement
   211→   211→          # feature flag (default OFF in source, ON when env var = 'true').
   212→   212→          #
   213→   213→          # Same create-env-var pattern as above: list → remove → create.
   214→   214→          # ----------------------------------------------------------------
   215→   215→          echo ""
   216→   216→          echo "  === Setting FEATURE_REQUEST_HASH_ENFORCEMENT=true on Vercel (preview+production) ==="
   217→   217→
   218→   218→          # Re-list env vars (to get fresh IDs after the EVIDENCE_TEST_MODE mutations above)
   219→   219→          LIST_RESP2=$(curl -sS -w "\n%{http_code}" -H "$AUTH" "$API_URL")
   220→   220→          LIST_HTTP2=$(echo "$LIST_RESP2" | tail -1)
   221→   221→          LIST_BODY2=$(echo "$LIST_RESP2" | head -n -1)
   222→   222→          if [ "$LIST_HTTP2" != "200" ]; then
   223→   223→            echo "  ❌ Failed to re-list env vars (HTTP $LIST_HTTP2)"
   224→   224→            echo "  Response: $(echo $LIST_BODY2 | head -c 500)"
   225→   225→            exit 1
   226→   226→          fi
   227→   227→
   228→   228→          # Show existing FEATURE_REQUEST_HASH_ENFORCEMENT entries (if any)
   229→   229→          echo "  === Existing FEATURE_REQUEST_HASH_ENFORCEMENT entries ==="
   230→   230→          echo "$LIST_BODY2" | jq -r '.envs[] | select(.key == "FEATURE_REQUEST_HASH_ENFORCEMENT") | "  - id=\(.id) target=\(.target | join(",")) type=\(.type) value=\(.value // "(encrypted)")"' 2>/dev/null || echo "  (none)"
   231→   231→
   232→   232→          # Remove existing FEATURE_REQUEST_HASH_ENFORCEMENT entries (both preview AND production)
   233→   233→          EXISTING_IDS2=$(echo "$LIST_BODY2" | jq -r '.envs[] | select(.key == "FEATURE_REQUEST_HASH_ENFORCEMENT") | .id' 2>/dev/null)
   234→   234→          if [ -n "$EXISTING_IDS2" ]; then
   235→   235→            for EID2 in $EXISTING_IDS2; do
   236→   236→              echo "  ⚠️  Removing existing FEATURE_REQUEST_HASH_ENFORCEMENT id=$EID2"
   237→   237→              curl -sS -X DELETE -H "$AUTH" "$API_URL/$EID2" > /dev/null 2>&1
   238→   238→            done
   239→   239→          fi
   240→   240→
   241→   241→          # Create FEATURE_REQUEST_HASH_ENFORCEMENT=true for BOTH preview AND production targets
   242→   242→          echo ""
   243→   243→          echo "  === Creating FEATURE_REQUEST_HASH_ENFORCEMENT=true for preview+production targets ==="
   244→   244→          CREATE_PAYLOAD2='{"key":"FEATURE_REQUEST_HASH_ENFORCEMENT","value":"true","target":["preview","production"],"type":"plain"}'
   245→   245→          echo "  Payload: $CREATE_PAYLOAD2"
   246→   246→
   247→   247→          RESULT2=$(curl -sS -w "\n%{http_code}" -X POST \
   248→   248→            -H "$AUTH" \
   249→   249→            -H "Content-Type: application/json" \
   250→   250→            "$API_URL" \
   251→   251→            -d "$CREATE_PAYLOAD2")
   252→   252→
   253→   253→          RESULT_HTTP2=$(echo "$RESULT2" | tail -1)
   254→   254→          RESULT_BODY2=$(echo "$RESULT2" | head -n -1)
   255→   255→          echo "  Create HTTP: $RESULT_HTTP2"
   256→   256→          echo "  Create response: $RESULT_BODY2" | head -c 500
   257→   257→          echo ""
   258→   258→
   259→   259→          if echo "$RESULT_HTTP2" | grep -qE "^(200|201)$" && echo "$RESULT_BODY2" | jq -e '.created.id // .id' > /dev/null 2>&1; then
   260→   260→            CREATED_ID2=$(echo "$RESULT_BODY2" | jq -r '.created.id // .id')
   261→   261→            echo "  ✅ FEATURE_REQUEST_HASH_ENFORCEMENT=true configured for preview+production environments (id=$CREATED_ID2)"
   262→   262→          else
   263→   263→            echo "  ❌ Failed to set FEATURE_REQUEST_HASH_ENFORCEMENT"
   264→   264→            echo "  HTTP code: $RESULT_HTTP2"
   265→   265→            echo "  Response body: $RESULT_BODY2"
   266→   266→            exit 1
   267→   267→          fi
   268→   268→
   269→   269→      - name: Trigger new Vercel deployment (to pick up EVIDENCE_TEST_MODE + FEATURE_REQUEST_HASH_ENFORCEMENT env vars)
   270→   270→        run: |
   271→   271→          set +e
   272→   272→          echo "=== Triggering new Vercel deployment from main branch ==="
   273→   273→          PROJECT_ID="$VERCEL_PROJECT_ID"
   274→   274→          AUTH="Authorization: Bearer $VERCEL_TOKEN"
   275→   275→
   276→   276→          # ------------------------------------------------------------------
   277→   277→          # Step 0: Query the Vercel project to get the linked GitHub repo's
   278→   278→          # numeric repoId (Vercel API v13/deployments REQUIRES gitSource.repoId
   279→   279→          # when gitSource.type=github). This is needed for the Option B fallback.
   280→   280→          # ------------------------------------------------------------------
   281→   281→          echo ""
   282→   282→          echo "  === Querying Vercel project for linked git source info ==="
   283→   283→          PROJECT_RESP=$(curl -sS -w "\n%{http_code}" -H "$AUTH" \
   284→   284→            "https://api.vercel.com/v9/projects/$PROJECT_ID")
   285→   285→          PROJECT_HTTP=$(echo "$PROJECT_RESP" | tail -1)
   286→   286→          PROJECT_BODY=$(echo "$PROJECT_RESP" | head -n -1)
   287→   287→          echo "  Project query HTTP: $PROJECT_HTTP"
   288→   288→          if [ "$PROJECT_HTTP" != "200" ]; then
   289→   289→            echo "  ❌ Failed to query Vercel project (HTTP $PROJECT_HTTP)"
   290→   290→            echo "  Response: $(echo "$PROJECT_BODY" | head -c 500)"
   291→   291→            exit 1
   292→   292→          fi
   293→   293→          LINK_TYPE=$(echo "$PROJECT_BODY" | jq -r '.link.type // empty')
   294→   294→          REPO_ID=$(echo "$PROJECT_BODY" | jq -r '.link.repoId // empty')
   295→   295→          ORG=$(echo "$PROJECT_BODY" | jq -r '.link.org // empty')
   296→   296→          REPO=$(echo "$PROJECT_BODY" | jq -r '.link.repo // empty')
   297→   297→          echo "  Linked git source:"
   298→   298→          echo "    type:   $LINK_TYPE"
   299→   299→          echo "    org:    $ORG"
   300→   300→          echo "    repo:   $REPO"
   301→   301→          echo "    repoId: $REPO_ID"
   302→   302→
   303→   303→          # Show recent READY preview deployments for context
   304→   304→          echo ""
   305→   305→          echo "  === Recent READY Vercel deployments (any target) ==="
   306→   306→          LIST_DEPLOYS=$(curl -sS -H "$AUTH" \
   307→   307→            "https://api.vercel.com/v6/deployments?projectId=$PROJECT_ID&limit=5&state=READY")
   308→   308→          echo "$LIST_DEPLOYS" | jq -r '.deployments[]? | "    - target=\(.target // "preview") url=\(.url) created=\(.createdAt) commit=\(.meta.githubCommitSha // "n/a" | .[0:8])"' 2>/dev/null | head -5
   309→   309→
   310→   310→          # ------------------------------------------------------------------
   311→   311→          # Step 1: Trigger a PREVIEW deployment (NOT production).
   312→   312→          #
   313→   313→          # Why preview (not production):
   314→   314→          #   1. Production deployments on this Vercel project CANNOT reach
   315→   315→          #      the Supabase pooler (verified — HTTP 503 'Can't reach database
   316→   316→          #      server at aws-ap-northeast-1.pooler.supabase.com:6543' on the
   317→   317→          #      production URL snakpass-dbp3ic55z-snakzap.vercel.app). Preview
   318→   318→          #      deployments (like snakpass-eqkarf10s-snakzap.vercel.app)
   319→   319→          #      CAN reach the DB — verified HTTP 200, latencyMs=12.
   320→   320→          #   2. NODE_ENV=development env var was set in Step #7 above (target
   321→   321→          #      preview+production). The evidence-setup endpoint guards on
   322→   322→          #      `process.env.NODE_ENV === 'production'` and refuses to serve
   323→   323→          #      in production mode. The new preview deployment should pick up
   324→   324→          #      NODE_ENV=development (if Vercel honors the override).
   325→   325→          #
   326→   326→          # API note on `target` field:
   327→   327→          #   - Vercel v13/deployments rejects `target=preview` with HTTP 400
   328→   328→          #     "Invalid request: `target` should be 'production', 'staging',
   329→   329→          #      or a custom environment identifier." (verified — see previous
   330→   330→          #      workflow run 31890722345).
   331→   331→          #   - To create a PREVIEW deployment, OMIT the `target` field
   332→   332→          #     entirely. Vercel defaults to preview when target is absent.
   333→   333→          #
   334→   334→          # gitSource requirement:
   335→   335→          #   - Vercel API REQUIRES gitSource.repoId (numeric GitHub repo ID)
   336→   336→          #     when gitSource.type=github. The previous workflow's bug was
   337→   337→          #     omitting repoId (HTTP 400 "gitSource missing required property
   338→   338→          #     repoId"). We extract repoId from GET /v9/projects/$PROJECT_ID
   339→   339→          #     link.repoId field (verified value: 1331058897 for zheoOviya/snakpass).
   340→   340→          # ------------------------------------------------------------------
   341→   341→          echo ""
   342→   342→          echo "  === Triggering PREVIEW deployment (target field omitted) ==="
   343→   343→          if [ -z "$REPO_ID" ] || [ "$REPO_ID" = "null" ]; then
   344→   344→            echo "  ❌ Cannot trigger deployment — project has no linked repoId."
   345→   345→            exit 1
   346→   346→          fi
   347→   347→          PAYLOAD=$(jq -n \
   348→   348→            --arg name "snakpass" \
   349→   349→            --arg type "$LINK_TYPE" \
   350→   350→            --arg org "$ORG" \
   351→   351→            --arg repo "$REPO" \
   352→   352→            --argjson repoId "$REPO_ID" \
   353→   353→            --arg ref "main" \
   354→   354→            '{name:$name, gitSource:{type:$type, org:$org, repo:$repo, repoId:$repoId, ref:$ref}}')
   355→   355→          echo "  Payload (note: no 'target' field => preview deployment):"
   356→   356→          echo "$PAYLOAD" | jq '.' | sed 's/^/    /'
   357→   357→
   358→   358→          DEPLOY_RESP=$(curl -sS -w "\n%{http_code}" -X POST \
   359→   359→            -H "$AUTH" \
   360→   360→            -H "Content-Type: application/json" \
   361→   361→            "https://api.vercel.com/v13/deployments" \
   362→   362→            -d "$PAYLOAD")
   363→   363→
   364→   364→          DEPLOY_HTTP=$(echo "$DEPLOY_RESP" | tail -1)
   365→   365→          DEPLOY_BODY=$(echo "$DEPLOY_RESP" | head -n -1)
   366→   366→          echo "  Deploy HTTP: $DEPLOY_HTTP"
   367→   367→          echo "  Deploy response (first 600 chars): $(echo "$DEPLOY_BODY" | head -c 600)"
   368→   368→
   369→   369→          DEPLOY_ID=$(echo "$DEPLOY_BODY" | jq -r '.id // empty')
   370→   370→
   371→   371→          DEPLOY_URL=$(echo "$DEPLOY_BODY" | jq -r '.url // empty')
   372→   372→
   373→   373→          if [ -n "$DEPLOY_ID" ] && [ "$DEPLOY_ID" != "null" ]; then
   374→   374→            echo ""
   375→   375→            echo "  ✅ Deployment triggered: id=$DEPLOY_ID, url=$DEPLOY_URL"
   376→   376→            if [ -n "$DEPLOY_URL" ] && [ "$DEPLOY_URL" != "null" ]; then
   377→   377→              NEW_STAGING_URL="https://$DEPLOY_URL"
   378→   378→              echo "NEW_STAGING_URL=$NEW_STAGING_URL" >> $GITHUB_ENV
   379→   379→              echo "  New staging URL: $NEW_STAGING_URL"
   380→   380→            fi
   381→   381→            echo "  Waiting for deployment to be READY..."
   382→   382→            for i in $(seq 1 60); do
   383→   383→              STATUS_RESP=$(curl -sS -H "$AUTH" "https://api.vercel.com/v13/deployments/$DEPLOY_ID")
   384→   384→              STATUS=$(echo "$STATUS_RESP" | jq -r '.status // empty')
   385→   385→              READY_STATE=$(echo "$STATUS_RESP" | jq -r '.readyState // empty')
   386→   386→              echo "    [$i] status=$STATUS, readyState=$READY_STATE"
   387→   387→              if [ "$READY_STATE" = "READY" ] || [ "$STATUS" = "READY" ]; then
   388→   388→                echo "  ✅ Deployment is READY"
   389→   389→                # Re-extract the URL in case it changed
   390→   390→                FINAL_URL=$(echo "$STATUS_RESP" | jq -r '.url // empty')
   391→   391→                if [ -n "$FINAL_URL" ] && [ "$FINAL_URL" != "null" ]; then
   392→   392→                  NEW_STAGING_URL="https://$FINAL_URL"
   393→   393→                  echo "NEW_STAGING_URL=$NEW_STAGING_URL" >> $GITHUB_ENV
   394→   394→                  echo "  Final staging URL: $NEW_STAGING_URL"
   395→   395→                fi
   396→   396→                break
   397→   397→              fi
   398→   398→              if [ "$READY_STATE" = "ERROR" ] || [ "$STATUS" = "ERROR" ] || [ "$STATUS" = "CANCELED" ]; then
   399→   399→                echo "  ❌ Deployment $READY_STATE"
   400→   400→                # Don't exit — fall back to the latest READY deployment
   401→   401→                break
   402→   402→              fi
   403→   403→              sleep 5
   404→   404→            done
   405→   405→          else
   406→   406→            echo "  ⚠️ Could not trigger new deployment."
   407→   407→            echo "  Falling back to querying the latest READY production deployment..."
   408→   408→          fi
   409→   409→
   410→   410→          # If we don't have a new deployment URL yet, query for the latest READY production deployment
   411→   411→          if [ -z "$NEW_STAGING_URL" ]; then
   412→   412→            echo ""
   413→   413→            echo "  === Querying latest READY production deployment ==="
   414→   414→            # Wait a bit for any in-progress deploy to complete
   415→   415→            sleep 30
   416→   416→            LATEST=$(curl -sS -H "$AUTH" \
   417→   417→              "https://api.vercel.com/v6/deployments?projectId=$PROJECT_ID&limit=10&target=production&state=READY")
   418→   418→            LATEST_URL=$(echo "$LATEST" | jq -r '.deployments[0].url // empty')
   419→   419→            LATEST_COMMIT=$(echo "$LATEST" | jq -r '.deployments[0].meta.githubCommitSha // "unknown"')
   420→   420→            LATEST_MSG=$(echo "$LATEST" | jq -r '.deployments[0].meta.githubCommitMessage // "unknown"' | head -c 80)
   421→   421→            echo "  Latest READY production deployment:"
   422→   422→            echo "    URL: https://$LATEST_URL"
   423→   423→            echo "    Commit SHA: $LATEST_COMMIT"
   424→   424→            echo "    Commit msg: $LATEST_MSG"
   425→   425→
   426→   426→            if [ -n "$LATEST_URL" ] && [ "$LATEST_URL" != "null" ]; then
   427→   427→              NEW_STAGING_URL="https://$LATEST_URL"
   428→   428→              echo "NEW_STAGING_URL=$NEW_STAGING_URL" >> $GITHUB_ENV
   429→   429→              echo "  ✅ Using latest READY production deployment: $NEW_STAGING_URL"
   430→   430→            fi
   431→   431→          fi
   432→   432→
   433→   433→      - name: Use new deployment URL if available
   434→   434→        run: |
   435→   435→          if [ -n "$NEW_STAGING_URL" ]; then
   436→   436→            echo "Using newly-triggered deployment URL: $NEW_STAGING_URL"
   437→   437→            echo "STAGING_URL=$NEW_STAGING_URL" >> $GITHUB_ENV
   438→   438→          else
   439→   439→            echo "Using provided staging URL: $STAGING_URL"
   440→   440→            echo "STAGING_URL=$STAGING_URL" >> $GITHUB_ENV
   441→   441→          fi
   442→   442→
   443→   443→      - name: Verify staging health + realPayments=false + evidence endpoints deployed
   444→   444→        run: |
   445→   445→          STAGING_URL="${STAGING_URL%/}"
   446→   446→          echo "=== Staging URL: $STAGING_URL ==="
   447→   447→
   448→   448→          # Retry the health check + evidence-setup endpoint a few times
   449→   449→          # (new deployments may take a moment to be fully reachable)
   450→   450→          READY=0
   451→   451→          for attempt in $(seq 1 12); do
   452→   452→            echo ""
   453→   453→            echo "=== Attempt $attempt: Health check ==="
   454→   454→            HEALTH=$(curl -sS -w "\n%{http_code}" "$STAGING_URL/api/health")
   455→   455→            HEALTH_HTTP=$(echo "$HEALTH" | tail -1)
   456→   456→            HEALTH_BODY=$(echo "$HEALTH" | head -n -1)
   457→   457→            echo "  Health HTTP: $HEALTH_HTTP"
   458→   458→            if [ "$HEALTH_HTTP" = "200" ]; then
   459→   459→              echo "  Health body: $HEALTH_BODY" | head -c 200
   460→   460→              echo ""
   461→   461→              echo "  === Verify evidence-setup endpoint ==="
   462→   462→              SETUP_RESP=$(curl -sS -w "\n%{http_code}" "$STAGING_URL/api/orders/evidence-setup?scenario=concurrent")
   463→   463→              SETUP_HTTP=$(echo "$SETUP_RESP" | tail -1)
   464→   464→              SETUP_BODY=$(echo "$SETUP_RESP" | head -n -1)
   465→   465→              echo "  evidence-setup HTTP: $SETUP_HTTP"
   466→   466→              if [ "$SETUP_HTTP" = "200" ]; then
   467→   467→                echo "  evidence-setup body: $SETUP_BODY" | head -c 200
   468→   468→                echo ""
   469→   469→                echo "✅ Evidence endpoints deployed + evidence test mode enabled"
   470→   470→                READY=1
   471→   471→                break
   472→   472→              elif [ "$SETUP_HTTP" = "403" ]; then
   473→   473→                echo "  ❌ Evidence test mode NOT enabled (403). Env var may not have propagated."
   474→   474→              else
   475→   475→                echo "  ⚠️ evidence-setup returned $SETUP_HTTP (expected 200). Latest commit may not be deployed yet."
   476→   476→              fi
   477→   477→            else
   478→   478→              echo "  ⚠️ Health check failed (HTTP $HEALTH_HTTP)"
   479→   479→            fi
   480→   480→            echo "  Waiting 15s before retry..."
   481→   481→            sleep 15
   482→   482→          done
   483→   483→
   484→   484→          if [ "$READY" != "1" ]; then
   485→   485→            echo ""
   486→   486→            echo "❌ FAILED: Staging not ready or evidence endpoints not deployed after 12 attempts."
   487→   487→            exit 1
   488→   488→          fi
   489→   489→
   490→   490→      - name: Run 5-concurrent-request test + verify PostgreSQL state
   491→   491→        env:
   492→   492→          # Use STAGING_URL from GITHUB_ENV (set by the deploy-trigger step to
   493→   493→          # the newly-deployed preview URL). Do NOT use inputs.staging_url here
   494→   494→          # because that is the stale default URL that doesn't have the
   495→   495→          # evidence endpoints deployed.
   496→   496→          STAGING_URL: ${{ env.STAGING_URL }}
   497→   497→        run: |
   498→   498→          STAGING_URL="${STAGING_URL%/}"
   499→   499→          RUN_ID="3c-pg-ev-$(date +%s)-$(echo $RANDOM | base64 | tr -dc 'a-z0-9' | head -c 8)"
   500→   500→          EVIDENCE_FILE="wave3-3c-postgresql-evidence.json"
   501→   501→
   502→   502→          echo "=================================================="
   503→   503→          echo "  Wave-3 Sub-Wave 3c — PostgreSQL C1 requestHash Evidence (Order POST)"
   504→   504→          echo "  Run ID: $RUN_ID"
   505→   505→          echo "  Staging URL: $STAGING_URL"
   506→   506→          echo "  Database: PostgreSQL (Supabase staging)"
   507→   507→          echo "  Feature flag: requestHashEnforcement=true (C1 active)"
   508→   508→          echo "=================================================="
   509→   509→          echo ""
   510→   510→
   511→   511→          # --- Step 1: Setup test scenario (create user + session) ---
   512→   512→          # NOTE: Unlike 3a (which pre-creates an Order), the 3b/3c setup endpoint
   513→   513→          # returns restaurantId + menuItemId + menuItemName + menuItemPrice
   514→   514→          # WITHOUT creating an Order — the POST /api/orders call IS what creates
   515→   515→          # the Order. There is no orderId in the setup response.
   516→   516→          echo "=== Step 1: Setup test scenario ==="
   517→   517→          SETUP_RESP=$(curl -sS -c /tmp/cookies.txt -b /tmp/cookies.txt "$STAGING_URL/api/orders/evidence-setup?scenario=concurrent")
   518→   518→          echo "Setup response: $SETUP_RESP"
   519→   519→
   520→   520→          USER_ID=$(echo "$SETUP_RESP" | jq -r '.userId')
   521→   521→          SESSION_TOKEN=$(echo "$SETUP_RESP" | jq -r '.sessionToken')
   522→   522→          CSRF_TOKEN=$(echo "$SETUP_RESP" | jq -r '.csrfToken')
   523→   523→          RESTAURANT_ID=$(echo "$SETUP_RESP" | jq -r '.restaurantId')
   524→   524→          MENU_ITEM_ID=$(echo "$SETUP_RESP" | jq -r '.menuItemId')
   525→   525→          MENU_ITEM_NAME=$(echo "$SETUP_RESP" | jq -r '.menuItemName')
   526→   526→          MENU_ITEM_PRICE=$(echo "$SETUP_RESP" | jq -r '.menuItemPrice')
   527→   527→
   528→   528→          if [ -z "$RESTAURANT_ID" ] || [ "$RESTAURANT_ID" = "null" ] || \
   529→   529→             [ -z "$MENU_ITEM_ID" ] || [ "$MENU_ITEM_ID" = "null" ]; then
   530→   530→            echo "❌ Failed to set up test scenario (missing restaurant/menuItem)"
   531→   531→            exit 1
   532→   532→          fi
   533→   533→          echo "✅ Test setup: restaurantId=$RESTAURANT_ID, menuItemId=$MENU_ITEM_ID, menuItemName=$MENU_ITEM_NAME, menuItemPrice=$MENU_ITEM_PRICE (user: $USER_ID)"
   534→   534→
   535→   535→          IDEMPOTENCY_KEY="ev-pg-concurrent-3c-$(date +%s)-$(echo $RANDOM | tr -dc 'a-z0-9' | head -c 6)"
   536→   536→
   537→   537→          echo ""
   538→   538→          echo "=== Step 2: Fire 5 concurrent POST /api/orders requests with same idempotency key (requestHashEnforcement=true) ==="
   539→   539→          echo "Idempotency key: $IDEMPOTENCY_KEY"
   540→   540→
   541→   541→          # Build the Order POST body once — all 5 concurrent requests use the SAME body
   542→   542→          # (same restaurantId + same item), which is the precondition for the
   543→   543→          # idempotency-key test: N concurrent identical POSTs → exactly 1 Order created.
   544→   544→          ORDER_BODY=$(jq -n \
   545→   545→            --arg restaurantId "$RESTAURANT_ID" \
   546→   546→            --arg menuItemId "$MENU_ITEM_ID" \
   547→   547→            --arg name "$MENU_ITEM_NAME" \
   548→   548→            --argjson price "$MENU_ITEM_PRICE" \
   549→   549→            '{restaurantId:$restaurantId, items:[{menuItemId:$menuItemId, name:$name, price:$price, quantity:1}]}')
   550→   550→          echo "Order POST body: $ORDER_BODY"
   551→   551→
   552→   552→          # Fire 5 concurrent requests in parallel using background jobs
   553→   553→          CONCURRENCY=5
   554→   554→          RESULTS_DIR="/tmp/concurrent-results"
   555→   555→          mkdir -p "$RESULTS_DIR"
   556→   556→
   557→   557→          for i in $(seq 1 $CONCURRENCY); do
   558→   558→            (
   559→   559→              RESP_FILE="$RESULTS_DIR/resp_$i.json"
   560→   560→              HTTP_FILE="$RESULTS_DIR/http_$i.txt"
   561→   561→
   562→   562→              HTTP_CODE=$(curl -sS -o "$RESP_FILE" -w "%{http_code}" \
   563→   563→                -X POST "$STAGING_URL/api/orders" \
   564→   564→                -H "Content-Type: application/json" \
   565→   565→                -H "Idempotency-Key: $IDEMPOTENCY_KEY" \
   566→   566→                -H "Cookie: snakzap_session=$SESSION_TOKEN; snakzap_csrf=$CSRF_TOKEN" \
   567→   567→                -H "X-CSRF-Token: $CSRF_TOKEN" \
   568→   568→                -d "$ORDER_BODY")
   569→   569→
   570→   570→              echo "$HTTP_CODE" > "$HTTP_FILE"
   571→   571→              echo "Request $i: HTTP $HTTP_CODE, body: $(cat $RESP_FILE | head -c 200)"
   572→   572→            ) &
   573→   573→          done
   574→   574→
   575→   575→          # Wait for all concurrent requests to complete
   576→   576→          wait
   577→   577→          echo "✅ All 5 concurrent requests completed"
   578→   578→
   579→   579→          echo ""
   580→   580→          echo "=== Step 3: Collect response results ==="
   581→   581→          ALL_RESPONSES="["
   582→   582→          for i in $(seq 1 $CONCURRENCY); do
   583→   583→            HTTP_CODE=$(cat "$RESULTS_DIR/http_$i.txt")
   584→   584→            BODY=$(cat "$RESULTS_DIR/resp_$i.json")
   585→   585→            ORDER_ID=$(echo "$BODY" | jq -r '.order.id // empty' 2>/dev/null)
   586→   586→            ERROR_CODE=$(echo "$BODY" | jq -r '.error.code // empty' 2>/dev/null)
   587→   587→            ERROR_MSG=$(echo "$BODY" | jq -r '.error.message // empty' 2>/dev/null)
   588→   588→
   589→   589→            REQUEST_ENTRY=$(jq -n \
   590→   590→              --argjson index "$i" \
   591→   591→              --argjson status "$HTTP_CODE" \
   592→   592→              --arg orderId "$ORDER_ID" \
   593→   593→              --arg errorCode "$ERROR_CODE" \
   594→   594→              --arg errorMsg "$ERROR_MSG" \
   595→   595→              '{requestIndex: $index, status: $status, orderId: $orderId, errorCode: $errorCode, errorMessage: $errorMsg}')
   596→   596→
   597→   597→            if [ "$i" = "1" ]; then
   598→   598→              ALL_RESPONSES="$ALL_RESPONSES$REQUEST_ENTRY"
   599→   599→            else
   600→   600→              ALL_RESPONSES="$ALL_RESPONSES,$REQUEST_ENTRY"
   601→   601→            fi
   602→   602→            echo "  Request $i: HTTP $HTTP_CODE, orderId=$ORDER_ID, error=$ERROR_CODE"
   603→   603→          done
   604→   604→          ALL_RESPONSES="$ALL_RESPONSES]"
   605→   605→
   606→   606→          # Compute unique order IDs from responses (non-empty only)
   607→   607→          UNIQUE_ORDER_IDS=$(echo "$ALL_RESPONSES" | jq -r '[.[].orderId | select(. != "")] | unique | length')
   608→   608→          SUCCESS_COUNT=$(echo "$ALL_RESPONSES" | jq -r '[.[] | select(.status == 200)] | length')
   609→   609→          ERROR_COUNT=$(echo "$ALL_RESPONSES" | jq -r '[.[] | select(.status != 200)] | length')
   610→   610→
   611→   611→          # 3c C1 — Count 422 (IdempotencyKeyReuseError) responses. Should be 0
   612→   612→          # when all 5 concurrent requests use the SAME body — the incoming hash
   613→   613→          # matches the stored hash, so all 5 return the cached 200 response.
   614→   614→          # A non-zero 422 count would indicate C1 hash enforcement is misfiring
   615→   615→          # (e.g. computing different hashes for the same body — a bug).
   616→   616→          COUNT_422=$(echo "$ALL_RESPONSES" | jq -r '[.[] | select(.status == 422)] | length')
   617→   617→          COUNT_REUSE_ERROR=$(echo "$ALL_RESPONSES" | jq -r '[.[] | select(.errorCode == "IDEMPOTENCY_KEY_REUSE")] | length')
   618→   618→          if [ "$COUNT_422" = "0" ] && [ "$COUNT_REUSE_ERROR" = "0" ]; then
   619→   619→            NO_422_ERRORS="true"
   620→   620→            NO_IDEMPOTENCY_KEY_REUSE_ERRORS="true"
   621→   621→          else
   622→   622→            NO_422_ERRORS="false"
   623→   623→            NO_IDEMPOTENCY_KEY_REUSE_ERRORS="false"
   624→   624→          fi
   625→   625→
   626→   626→          # Pick the "winning" orderId (the first non-empty orderId in the responses).
   627→   627→          # This is the orderId we'll use for DB verification queries.
   628→   628→          ORDER_ID=$(echo "$ALL_RESPONSES" | jq -r '[.[].orderId | select(. != "")] | .[0] // empty')
   629→   629→
   630→   630→          echo ""
   631→   631→          echo "Summary of responses:"
   632→   632→          echo "  Success count: $SUCCESS_COUNT"
   633→   633→          echo "  Error count: $ERROR_COUNT"
   634→   634→          echo "  Unique order IDs in responses: $UNIQUE_ORDER_IDS"
   635→   635→          echo "  Winning orderId (used for DB verification): $ORDER_ID"
   636→   636→
   637→   637→          if [ -z "$ORDER_ID" ] || [ "$ORDER_ID" = "null" ]; then
   638→   638→            echo "❌ No orderId returned by any request — cannot verify DB state"
   639→   639→            exit 1
   640→   640→          fi
   641→   641→
   642→   642→          echo ""
   643→   643→          echo "=== Step 4: Verify PostgreSQL database state directly (via Supabase Management API) ==="
   644→   644→
   645→   645→          # Query Order table
   646→   646→          ORDER_SQL=$(printf 'SELECT "id", "status", "totalAmount", "itemsCount" FROM "Order" WHERE "id" = '\''%s'\'';' "$ORDER_ID")
   647→   647→          ORDER_RESULT=$(curl -sS -X POST \
   648→   648→            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
   649→   649→            -H "Content-Type: application/json" \
   650→   650→            "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
   651→   651→            -d "$(jq -n --arg q "$ORDER_SQL" '{query: $q}')")
   652→   652→          echo "Order raw: $(echo $ORDER_RESULT | head -c 500)"
   653→   653→          echo ""
   654→   654→          # Supabase Management API may return array OR object depending on result shape.
   655→   655→          # Handle both: if array, take [0].field; if object, take .field.
   656→   656→          ORDER_COUNT=$(echo "$ORDER_RESULT" | jq -r 'if type == "array" then length else 0 end' 2>/dev/null || echo "0")
   657→   657→          ORDER_DB_STATUS=$(echo "$ORDER_RESULT" | jq -r 'if type == "array" then .[0].status // empty else .status // empty end' 2>/dev/null || echo "")
   658→   658→          ORDER_DB_TOTAL=$(echo "$ORDER_RESULT" | jq -r 'if type == "array" then .[0].totalAmount // empty else .totalAmount // empty end' 2>/dev/null || echo "")
   659→   659→          ORDER_DB_ITEMS_COUNT=$(echo "$ORDER_RESULT" | jq -r 'if type == "array" then .[0].itemsCount // empty else .itemsCount // empty end' 2>/dev/null || echo "")
   660→   660→
   661→   661→          # Query OrderItem count for this order
   662→   662→          ORDER_ITEM_SQL=$(printf 'SELECT COUNT(*) as count FROM "OrderItem" WHERE "orderId" = '\''%s'\'';' "$ORDER_ID")
   663→   663→          ORDER_ITEM_RESULT=$(curl -sS -X POST \
   664→   664→            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
   665→   665→            -H "Content-Type: application/json" \
   666→   666→            "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
   667→   667→            -d "$(jq -n --arg q "$ORDER_ITEM_SQL" '{query: $q}')")
   668→   668→          echo "OrderItem raw: $(echo $ORDER_ITEM_RESULT | head -c 300)"
   669→   669→          ORDER_ITEM_COUNT=$(echo "$ORDER_ITEM_RESULT" | jq -r 'if type == "array" then .[0].count // 0 else .count // 0 end' 2>/dev/null || echo "0")
   670→   670→
   671→   671→          # Query Outbox count (aggregateType='Order', aggregateId=<orderId>)
   672→   672→          OUTBOX_SQL=$(printf 'SELECT COUNT(*) as count FROM "Outbox" WHERE "aggregateType" = '\''Order'\'' AND "aggregateId" = '\''%s'\'';' "$ORDER_ID")
   673→   673→          OUTBOX_RESULT=$(curl -sS -X POST \
   674→   674→            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
   675→   675→            -H "Content-Type: application/json" \
   676→   676→            "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
   677→   677→            -d "$(jq -n --arg q "$OUTBOX_SQL" '{query: $q}')")
   678→   678→          echo "Outbox raw: $(echo $OUTBOX_RESULT | head -c 300)"
   679→   679→          OUTBOX_COUNT=$(echo "$OUTBOX_RESULT" | jq -r 'if type == "array" then .[0].count // 0 else .count // 0 end' 2>/dev/null || echo "0")
   680→   680→
   681→   681→          # Query IdempotencyKey count for this key
   682→   682→          IDEM_SQL=$(printf 'SELECT COUNT(*) as count FROM "IdempotencyKey" WHERE "key" = '\''%s'\'';' "$IDEMPOTENCY_KEY")
   683→   683→          IDEM_RESULT=$(curl -sS -X POST \
   684→   684→            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
   685→   685→            -H "Content-Type: application/json" \
   686→   686→            "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
   687→   687→            -d "$(jq -n --arg q "$IDEM_SQL" '{query: $q}')")
   688→   688→          echo "Idempotency raw: $(echo $IDEM_RESULT | head -c 300)"
   689→   689→          IDEM_COUNT=$(echo "$IDEM_RESULT" | jq -r 'if type == "array" then .[0].count // 0 else .count // 0 end' 2>/dev/null || echo "0")
   690→   690→
   691→   691→          # 3c C1 — Query IdempotencyKey.requestHash for this key (MUST be non-null
   692→   692→          # when requestHashEnforcement=true, since the hash is computed by
   693→   693→          # computeRequestHash() + stored by storeIdempotencyRecord() alongside
   694→   694→          # the cached response). Null requestHash would indicate C1 hash storage
   695→   695→          # did NOT happen (e.g. flag not actually ON, or storeIdempotencyRecord
   696→   696→          # was called with requestHash=null).
   697→   697→          IDEM_HASH_SQL=$(printf 'SELECT "requestHash" FROM "IdempotencyKey" WHERE "key" = '\''%s'\'';' "$IDEMPOTENCY_KEY")
   698→   698→          IDEM_HASH_RESULT=$(curl -sS -X POST \
   699→   699→            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
   700→   700→            -H "Content-Type: application/json" \
   701→   701→            "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
   702→   702→            -d "$(jq -n --arg q "$IDEM_HASH_SQL" '{query: $q}')")
   703→   703→          echo "Idempotency requestHash raw: $(echo $IDEM_HASH_RESULT | head -c 300)"
   704→   704→          IDEM_REQUEST_HASH=$(echo "$IDEM_HASH_RESULT" | jq -r 'if type == "array" then .[0].requestHash // empty else .requestHash // empty end' 2>/dev/null || echo "")
   705→   705→          if [ -n "$IDEM_REQUEST_HASH" ] && [ "$IDEM_REQUEST_HASH" != "null" ]; then
   706→   706→            REQUEST_HASH_STORED="true"
   707→   707→            echo "  ✅ IdempotencyKey.requestHash is non-null: ${IDEM_REQUEST_HASH:0:16}..."
   708→   708→          else
   709→   709→            REQUEST_HASH_STORED="false"
   710→   710→            echo "  ❌ IdempotencyKey.requestHash is null or missing (C1 hash storage did NOT happen)"
   711→   711→          fi
   712→   712→
   713→   713→          # Query AuditLog count for ORDER_CREATED for this order
   714→   714→          # NOTE: printf interprets % as format spec — use %% to emit a literal %
   715→   715→          AUDIT_SQL=$(printf 'SELECT COUNT(*) as count FROM "AuditLog" WHERE "action" = '\''ORDER_CREATED'\'' AND "metadata" LIKE '\''%%%s%%'\'';' "$ORDER_ID")
   716→   716→          AUDIT_RESULT=$(curl -sS -X POST \
   717→   717→            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
   718→   718→            -H "Content-Type: application/json" \
   719→   719→            "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
   720→   720→            -d "$(jq -n --arg q "$AUDIT_SQL" '{query: $q}')")
   721→   721→          echo "Audit raw: $(echo $AUDIT_RESULT | head -c 300)"
   722→   722→          AUDIT_COUNT=$(echo "$AUDIT_RESULT" | jq -r 'if type == "array" then .[0].count // 0 else .count // 0 end' 2>/dev/null || echo "0")
   723→   723→
   724→   724→          # Query ALL orders created by the test user (should be exactly 1 if idempotency held)
   725→   725→          USER_ORDER_SQL=$(printf 'SELECT COUNT(*) as count FROM "Order" WHERE "userId" = '\''%s'\'';' "$USER_ID")
   726→   726→          USER_ORDER_RESULT=$(curl -sS -X POST \
   727→   727→            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
   728→   728→            -H "Content-Type: application/json" \
   729→   729→            "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
   730→   730→            -d "$(jq -n --arg q "$USER_ORDER_SQL" '{query: $q}')")
   731→   731→          echo "Orders by test user raw: $(echo $USER_ORDER_RESULT | head -c 300)"
   732→   732→          USER_ORDER_COUNT=$(echo "$USER_ORDER_RESULT" | jq -r 'if type == "array" then .[0].count // 0 else .count // 0 end' 2>/dev/null || echo "0")
   733→   733→
   734→   734→          echo ""
   735→   735→          echo "=== Step 5: Database state summary ==="
   736→   736→          echo "  Order count (by orderId):       $ORDER_COUNT (expected: 1)"
   737→   737→          echo "  Order status:                   $ORDER_DB_STATUS (expected: CONFIRMED)"
   738→   738→          echo "  Order totalAmount:              $ORDER_DB_TOTAL"
   739→   739→          echo "  Order itemsCount:               $ORDER_DB_ITEMS_COUNT"
   740→   740→          echo "  OrderItem rows:                 $ORDER_ITEM_COUNT (expected: >=1)"
   741→   741→          echo "  Outbox event count:             $OUTBOX_COUNT (expected: 1)"
   742→   742→          echo "  Idempotency record count:       $IDEM_COUNT (expected: 1)"
   743→   743→          echo "  Audit log (ORDER_CREATED):      $AUDIT_COUNT (expected: 1)"
   744→   744→          echo "  Total orders by test user:      $USER_ORDER_COUNT (expected: 1 — idempotency held)"
   745→   745→          echo "  Unique order IDs in responses:  $UNIQUE_ORDER_IDS (expected: 1)"
   746→   746→          echo "  IdempotencyKey.requestHash stored: $REQUEST_HASH_STORED (expected: true — C1)"
   747→   747→          echo "  422 responses (IDEMPOTENCY_KEY_REUSE): $COUNT_422 (expected: 0 — same body)"
   748→   748→
   749→   749→          echo ""
   750→   750→          echo "=== Step 6: Verify exactly-one-order + C1 requestHash invariants ==="
   751→   751→          EXACTLY_ONE_ORDER="false"
   752→   752→          # Invariant: 1 Order + >=1 OrderItem + 1 Outbox + 1 IdempotencyKey + 1 AuditLog
   753→   753→          # + uniqueOrderIds=1 + Order.status=CONFIRMED + only 1 order by test user
   754→   754→          # + 3c C1: requestHash stored (non-null) + no 422 errors (all 5 returned 200).
   755→   755→          if [ "$ORDER_COUNT" = "1" ] && \
   756→   756→             [ "$ORDER_DB_STATUS" = "CONFIRMED" ] && \
   757→   757→             [ "$ORDER_ITEM_COUNT" -ge "1" ] && \
   758→   758→             [ "$OUTBOX_COUNT" = "1" ] && \
   759→   759→             [ "$IDEM_COUNT" = "1" ] && \
   760→   760→             [ "$AUDIT_COUNT" = "1" ] && \
   761→   761→             [ "$UNIQUE_ORDER_IDS" = "1" ] && \
   762→   762→             [ "$REQUEST_HASH_STORED" = "true" ] && \
   763→   763→             [ "$NO_422_ERRORS" = "true" ]; then
   764→   764→            EXACTLY_ONE_ORDER="true"
   765→   765→            echo "✅ PASS: exactly 1 Order, >=1 OrderItem, 1 Outbox, 1 IdempotencyKey, 1 AuditLog, 1 unique orderId, requestHash stored, no 422 errors"
   766→   766→          else
   767→   767→            echo "❌ FAIL: invariant violated"
   768→   768→          fi
   769→   769→
   770→   770→          echo ""
   771→   771→          echo "=== Step 7: Capture P2002 behavior of losing concurrent transactions ==="
   772→   772→          # The losing transactions should have received either:
   773→   773→          #   - HTTP 200 with the cached orderId (via retry → getCachedResponse)
   774→   774→          #   - HTTP 409 CONFLICT (if retry exhausted)
   775→   775→          # Both are acceptable — what matters is NO duplicate Order was created.
   776→   776→          LOSING_BEHAVIOR=$(echo "$ALL_RESPONSES" | jq -r '[.[] | select(.orderId != "" and .orderId == "'"$ORDER_ID"'") | {status, orderId, errorCode}]')
   777→   777→          echo "Losing transactions (responses with the winning orderId):"
   778→   778→          echo "$LOSING_BEHAVIOR" | jq '.' 2>/dev/null || echo "$LOSING_BEHAVIOR"
   779→   779→
   780→   780→          # Count responses that returned the winning orderId (should be all that returned 200)
   781→   781→          WINNING_RESPONSES=$(echo "$ALL_RESPONSES" | jq -r "[.[] | select(.orderId == \"$ORDER_ID\")] | length")
   782→   782→          echo ""
   783→   783→          echo "Responses returning winning orderId: $WINNING_RESPONSES"
   784→   784→          echo "Responses with errors (P2002/409): $ERROR_COUNT"
   785→   785→
   786→   786→          echo ""
   787→   787→          echo "=== Step 8: Generate self-validating evidence JSON ==="
   788→   788→
   789→   789→          # Build the evidence JSON (ok:true only if all invariants hold)
   790→   790→          jq -n \
   791→   791→            --argjson ok "$EXACTLY_ONE_ORDER" \
   792→   792→            --arg runId "$RUN_ID" \
   793→   793→            --arg timestamp "$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
   794→   794→            --arg database "postgresql" \
   795→   795→            --arg stagingUrl "$STAGING_URL" \
   796→   796→            --arg orderId "$ORDER_ID" \
   797→   797→            --arg userId "$USER_ID" \
   798→   798→            --arg restaurantId "$RESTAURANT_ID" \
   799→   799→            --arg menuItemId "$MENU_ITEM_ID" \
   800→   800→            --arg idempotencyKey "$IDEMPOTENCY_KEY" \
   801→   801→            --arg idempotencyRequestHash "$IDEM_REQUEST_HASH" \
   802→   802→            --arg orderStatus "$ORDER_DB_STATUS" \
   803→   803→            --arg orderTotalAmount "$ORDER_DB_TOTAL" \
   804→   804→            --argjson orderItemsCount "$ORDER_DB_ITEMS_COUNT" \
   805→   805→            --argjson concurrentRequests "$CONCURRENCY" \
   806→   806→            --argjson uniqueOrderIds "$UNIQUE_ORDER_IDS" \
   807→   807→            --argjson orderCount "$ORDER_COUNT" \
   808→   808→            --argjson orderItemCount "$ORDER_ITEM_COUNT" \
   809→   809→            --argjson outboxEventCount "$OUTBOX_COUNT" \
   810→   810→            --argjson idempotencyRecordCount "$IDEM_COUNT" \
   811→   811→            --argjson auditLogCount "$AUDIT_COUNT" \
   812→   812→            --argjson totalOrdersByUser "$USER_ORDER_COUNT" \
   813→   813→            --argjson successCount "$SUCCESS_COUNT" \
   814→   814→            --argjson errorCount "$ERROR_COUNT" \
   815→   815→            --argjson count422 "$COUNT_422" \
   816→   816→            --argjson requestHashStored "$REQUEST_HASH_STORED" \
   817→   817→            --argjson no422Errors "$NO_422_ERRORS" \
   818→   818→            --argjson noIdempotencyKeyReuseErrors "$NO_IDEMPOTENCY_KEY_REUSE_ERRORS" \
   819→   819→            --argjson winningResponses "$WINNING_RESPONSES" \
   820→   820→            --argjson responses "$ALL_RESPONSES" \
   821→   821→            --argjson losingBehavior "$LOSING_BEHAVIOR" \
   822→   822→            '{
   823→   823→              ok: $ok,
   824→   824→              runId: $runId,
   825→   825→              timestamp: $timestamp,
   826→   826→              wave: "3",
   827→   827→              subWave: "3c",
   828→   828→              evidenceType: "postgresql-concurrent-idempotency-requesthash",
   829→   829→              database: $database,
   830→   830→              stagingUrl: $stagingUrl,
   831→   831→              test: "5 concurrent POST /api/orders with same Idempotency-Key + requestHashEnforcement=true",
   832→   832→              orchestratorRequiredFields: {
   833→   833→                database: $database,
   834→   834→                concurrentRequests: $concurrentRequests,
   835→   835→                uniqueOrderIds: $uniqueOrderIds,
   836→   836→                orderCount: $orderCount,
   837→   837→                orderItemCount: $orderItemCount,
   838→   838→                outboxEventCount: $outboxEventCount,
   839→   839→                idempotencyRecordCount: $idempotencyRecordCount,
   840→   840→                auditLogCount: $auditLogCount,
   841→   841→                requestHashStored: $requestHashStored,
   842→   842→                noIdempotencyKeyReuseErrors: $noIdempotencyKeyReuseErrors
   843→   843→              },
   844→   844→              invariant: {
   845→   845→                exactlyOneOrder: $ok,
   846→   846→                requestHashStored: $requestHashStored,
   847→   847→                no422Errors: $no422Errors,
   848→   848→                description: "From 5 concurrent POST /api/orders requests with same idempotency key + requestHashEnforcement=true: exactly 1 Order, >=1 OrderItem, 1 outbox event, 1 idempotency record (with non-null requestHash), 1 audit log, all 5 responses 200 (no 422 IdempotencyKeyReuseError)"
   849→   849→              },
   850→   850→              setup: {
   851→   851→                userId: $userId,
   852→   852→                restaurantId: $restaurantId,
   853→   853→                menuItemId: $menuItemId,
   854→   854→                idempotencyKey: $idempotencyKey,
   855→   855→                concurrency: $concurrentRequests,
   856→   856→                requestHashEnforcement: true
   857→   857→              },
   858→   858→              responseSummary: {
   859→   859→                successCount: $successCount,
   860→   860→                errorCount: $errorCount,
   861→   861→                count422: $count422,
   862→   862→                no422Errors: $no422Errors,
   863→   863→                noIdempotencyKeyReuseErrors: $noIdempotencyKeyReuseErrors,
   864→   864→                uniqueOrderIdsInResponses: $uniqueOrderIds,
   865→   865→                winningOrderId: $orderId,
   866→   866→                responsesReturningWinningOrderId: $winningResponses,
   867→   867→                losingBehavior: $losingBehavior
   868→   868→              },
   869→   869→              databaseState: {
   870→   870→                orderCount: $orderCount,
   871→   871→                orderId: $orderId,
   872→   872→                orderStatus: $orderStatus,
   873→   873→                orderTotalAmount: $orderTotalAmount,
   874→   874→                orderItemsCount: $orderItemsCount,
   875→   875→                orderItemCount: $orderItemCount,
   876→   876→                outboxEventCount: $outboxEventCount,
   877→   877→                idempotencyRecordCount: $idempotencyRecordCount,
   878→   878→                idempotencyRequestHash: $idempotencyRequestHash,
   879→   879→                requestHashStored: $requestHashStored,
   880→   880→                auditLogCount: $auditLogCount,
   881→   881→                totalOrdersByUser: $totalOrdersByUser
   882→   882→              },
   883→   883→              expected: {
   884→   884→                orderCount: 1,
   885→   885→                orderStatus: "CONFIRMED",
   886→   886→                orderItemCount: ">=1",
   887→   887→                outboxEventCount: 1,
   888→   888→                idempotencyRecordCount: 1,
   889→   889→                auditLogCount: 1,
   890→   890→                uniqueOrderIds: 1,
   891→   891→                totalOrdersByUser: 1,
   892→   892→                requestHashStored: true,
   893→   893→                count422: 0
   894→   894→              },
   895→   895→              governance: {
   896→   896→                realPaymentsEnabled: false,
   897→   897→                productionRazorpayCredentialsUsed: false,
   898→   898→                webhookHandlerImplemented: false,
   899→   899→                subWave3cStarted: true,
   900→   900→                requestHashEnforcementEnabled: true,
   901→   901→                requestHashEnforcementScope: "Vercel preview+production env vars (set by this workflow)",
   902→   902→                productionTouched: false,
   903→   903→                note: "3c C1 evidence: 5 concurrent POST /api/orders with same Idempotency-Key + requestHashEnforcement=true (FEATURE_REQUEST_HASH_ENFORCEMENT=true on Vercel). Test run against staging Supabase PostgreSQL (project ref zmzqqcyapcezmaqvuzzd). realPayments=false (demo mode). No production traffic touched. Verifies C1 hash storage + no false-positive 422 IdempotencyKeyReuseError on concurrent same-body requests (P0-17 / P0-08)."
   904→   904→              },
   905→   905→              concurrentResponses: $responses
   906→   906→            }' > "$EVIDENCE_FILE"
   907→   907→
   908→   908→          echo "=== Evidence JSON ==="
   909→   909→          cat "$EVIDENCE_FILE" | jq '.'
   910→   910→
   911→   911→          echo ""
   912→   912→          echo "=== Step 9: Cleanup test data ==="
   913→   913→          # Delete the test OrderItem, Outbox, IdempotencyKey, AuditLog, Order.
   914→   914→          # (Leave the test user for reuse — 3b evidence-setup uses a stable phone.)
   915→   915→          CLEANUP_SQL=$(printf 'DELETE FROM "OrderItem" WHERE "orderId" = '\''%s'\''; DELETE FROM "Outbox" WHERE "aggregateType" = '\''Order'\'' AND "aggregateId" = '\''%s'\''; DELETE FROM "IdempotencyKey" WHERE "key" = '\''%s'\''; DELETE FROM "AuditLog" WHERE "action" = '\''ORDER_CREATED'\'' AND "metadata" LIKE '\''%%%s%%'\''; DELETE FROM "Order" WHERE "id" = '\''%s'\'';' "$ORDER_ID" "$ORDER_ID" "$IDEMPOTENCY_KEY" "$ORDER_ID" "$ORDER_ID")
   916→   916→          echo "Cleaning up test data..."
   917→   917→          curl -sS -X POST \
   918→   918→            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
   919→   919→            -H "Content-Type: application/json" \
   920→   920→            "https://api.supabase.com/v1/projects/$PROJECT_REF/database/query" \
   921→   921→            -d "$(jq -n --arg q "$CLEANUP_SQL" '{query: $q}')" > /dev/null 2>&1
   922→   922→          echo "✅ Cleanup complete"
   923→   923→
   924→   924→          echo ""
   925→   925→          echo "=================================================="
   926→   926→          echo "  EVIDENCE SUMMARY"
   927→   927→          echo "=================================================="
   928→   928→          echo "Run ID: $RUN_ID"
   929→   929→          echo "Database: PostgreSQL (staging Supabase)"
   930→   930→          echo "ok: $EXACTLY_ONE_ORDER"
   931→   931→          echo "Concurrent requests: $CONCURRENCY"
   932→   932→          echo "Unique order IDs: $UNIQUE_ORDER_IDS (expected: 1)"
   933→   933→          echo "Order count: $ORDER_COUNT (expected: 1)"
   934→   934→          echo "OrderItem count: $ORDER_ITEM_COUNT (expected: >=1)"
   935→   935→          echo "Outbox event count: $OUTBOX_COUNT (expected: 1)"
   936→   936→          echo "Idempotency record count: $IDEM_COUNT (expected: 1)"
   937→   937→          echo "Audit log count: $AUDIT_COUNT (expected: 1)"
   938→   938→          echo "requestHash stored: $REQUEST_HASH_STORED (expected: true)"
   939→   939→          echo "422 errors (IDEMPOTENCY_KEY_REUSE): $COUNT_422 (expected: 0)"
   940→   940→          echo "=================================================="
   941→   941→
   942→   942→          if [ "$EXACTLY_ONE_ORDER" != "true" ]; then
   943→   943→            echo "❌ EVIDENCE FAILED — invariant violated"
   944→   944→            exit 1
   945→   945→          fi
   946→   946→          echo "✅ EVIDENCE PASSED — exactly 1 Order from 5 concurrent POST /api/orders on PostgreSQL with requestHashEnforcement=true (C1)"
   947→   947→
   948→   948→      - name: Upload evidence
   949→   949→        if: always()
   950→   950→        uses: actions/upload-artifact@v4
   951→   951→        with:
   952→   952→          name: wave3-3c-postgresql-concurrent-evidence
   953→   953→          path: wave3-3c-postgresql-evidence.json
   954→   954→          retention-days: 90
   955→   955→