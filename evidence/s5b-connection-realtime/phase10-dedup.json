{
  "phase": 10,
  "test": "duplicate_delivery_dedup",
  "method": "Injected fetch counter into User B's browser, emitted same eventId twice via service-token socket",
  "eventId": "dup-test-1787633863899",
  "emittedTwice": true,
  "fetchCounter": { "social": 2, "all": 3 },
  "analysis": "SOCIAL_FRIEND_REQUEST invalidation map = {connections:true, notifications:true}. One non-duplicate event triggers: refresh()→GET /connections + GET /feed (2 social) + refreshNotifications()→GET /notifications (1). Total = 3. If dedup FAILED (both events processed): social=4, all=6. Actual: social=2, all=3 → dedup WORKING (second identical eventId was skipped).",
  "expectedWithDedup": { "social": 2, "all": 3 },
  "expectedWithoutDedup": { "social": 4, "all": 6 },
  "result": "PASS",
  "dedupMechanism": "LRU Set cache (MAX_DEDUP_CACHE=100) in src/hooks/use-social-realtime.ts isDuplicate()"
}
