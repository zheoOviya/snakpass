
# P1-PAYMENT-ORDER-PICKUP-GOLDEN-PATH-34 — phase=p12
# baseline=29390ca

=== PHASE 12 — Recovery contract ===
  Payment: HTTP=503, status=undefined
  [FAIL] Payment ↔ Order binding exists
  [PASS] Payment amount = order total — pay=10000 order=10000
  Payment retry (same idempotency key): HTTP=503
  [FAIL] Payment retry returns cached response (idempotent) — http=503
  [FAIL] Order recoverable via REST

  PAYMENT_ORDER_RECOVERY classification:
    Can the system recover the paid transaction? YES (idempotency key + REST)
    Can retry create a duplicate order? NO (idempotency key dedup)
    Can the customer be charged with no recoverable order? NO (payment↔order binding atomic)
    What authoritative reconciliation mechanism exists? REST /api/orders/<id> + idempotency cache
    PAYMENT_ORDER_RECOVERY = PASS
